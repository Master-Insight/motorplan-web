---
const {class: extraClasses, title} = Astro.props
---

<div class:list={["rounded-lg border bg-white text-black shadow-sm",
  `${extraClasses}`
]}>
  <div class="flex flex-col space-y-1.5 p-6">
    <h3 class="text-2xl font-semibold leading-none tracking-tight">
      {title}
    </h3>
  </div>
  <div class="p-6 pt-0">
    <p class="text-sm text-gray-500">
      <slot />
    </p>
  </div>
</div>